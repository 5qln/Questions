## New question(s)

## Checklist
- [ ] `python bin/membrane_lint.py` passes
- [ ] `python -m pytest` passes
- [ ] Only `# X` and `## ∞0' —` content; no answers, no authors, no identities
- [ ] File is at `questions/<xx>/<yy>/<hash>.md` matching its content hash
